// Commands module barrel. The dispatchable command catalog lives in catalog.ts (single-sourced);
// re-exported here as the module's external surface.
export { COMMAND_CATALOG } from './catalog.ts';
